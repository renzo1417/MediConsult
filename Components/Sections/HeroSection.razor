<section class="max-w-7xl mx-auto px-6 pt-12 pb-20">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
        <!-- Left Column -->
        <div class="lg:col-span-7 space-y-8">
            <h1 class="text-5xl sm:text-6xl font-bold tracking-tight text-neutral-900 leading-[1.15]">
                Your health,<br />
                <span class="inline-block bg-brand-lime px-3 py-1 rounded-xl text-neutral-950">expert care,</span><br />
                at your door.
            </h1>

            <p class="text-neutral-500 text-base sm:text-lg max-w-xl leading-relaxed">
                Personalized home doctor visits, video consultations, and rapid prescription deliveries 
                designed around your daily schedule.
            </p>

            <div class="flex flex-wrap items-center gap-4">
                <a href="#book" class="px-7 py-3.5 rounded-full bg-brand-dark text-white text-sm font-semibold hover:bg-neutral-800 transition">
                    Book a Consultation
                </a>
                <a href="#video" class="px-7 py-3.5 rounded-full border border-neutral-300 text-sm font-semibold text-neutral-800 hover:border-black transition flex items-center space-x-2">
                    <span>Watch Video</span>
                </a>
            </div>

            <!-- Stats -->
            <div class="pt-8 grid grid-cols-4 gap-4 border-t border-neutral-200">
                <div>
                    <p class="text-2xl font-bold text-neutral-900">50K+</p>
                    <p class="text-xs text-neutral-500 mt-1">Active Patients</p>
                </div>
                <div>
                    <p class="text-2xl font-bold text-neutral-900">200+</p>
                    <p class="text-xs text-neutral-500 mt-1">Top Doctors</p>
                </div>
                <div>
                    <p class="text-2xl font-bold text-neutral-900">4.9★</p>
                    <p class="text-xs text-neutral-500 mt-1">Customer Rating</p>
                </div>
                <div>
                    <p class="text-2xl font-bold text-neutral-900">24/7</p>
                    <p class="text-xs text-neutral-500 mt-1">Support Care</p>
                </div>
            </div>
        </div>

        <!-- Right Column (Image + Floating Badge) -->
        <div class="lg:col-span-5 relative">
            <div class="relative overflow-hidden rounded-3xl aspect-[4/5] bg-neutral-100 shadow-xl">
               <img src="images/hero-doctor.png" 
                    alt="Doctor consultation" 
                    class="w-full h-full object-cover" />
                
                <!-- Floating Card -->
                <div class="absolute bottom-6 left-6 bg-brand-dark/95 backdrop-blur-md p-4 rounded-2xl text-white shadow-2xl flex items-center space-x-4 border border-white/10">
                    <div class="w-10 h-10 rounded-full bg-brand-lime flex items-center justify-center text-neutral-950 font-bold">
                        ✓
                    </div>
                    <div>
                        <p class="text-xs text-neutral-400">Instant Telehealth</p>
                        <p class="text-sm font-semibold text-white">Call Started</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>